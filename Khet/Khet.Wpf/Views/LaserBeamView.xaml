<UserControl x:Class="Khet.Wpf.Views.LaserBeamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Khet.Wpf.Views"
             mc:Ignorable="d" 
             Height="100" Width="100">
    <Grid>
        <Rectangle Fill="Red" Height="{Binding height[0]}" Width="{Binding width[0]}" HorizontalAlignment="{Binding horizontalAlignment[0]}" VerticalAlignment="{Binding verticalAlignment[0]}"/>
        <Rectangle Fill="Red" Height="{Binding height[1]}" Width="{Binding width[1]}" HorizontalAlignment="{Binding horizontalAlignment[1]}" VerticalAlignment="{Binding verticalAlignment[1]}"/>
        
    </Grid>
</UserControl>
